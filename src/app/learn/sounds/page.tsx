import { SoundsPrimer } from "@/features/primer/sounds-primer";

export const metadata = { title: "Meet the Sounds — Japalingo" };

export default function SoundsPage() {
  return <SoundsPrimer />;
}
